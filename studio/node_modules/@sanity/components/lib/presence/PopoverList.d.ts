import React from 'react';
import { Position } from 'react-tippy';
import { User } from './types';
declare type Props<Item> = {
    items: Item[];
    renderItem: (item: Item, onClose?: (event: any) => void) => React.ReactNode;
    position?: Position;
    trigger?: 'mouseenter' | 'click' | 'manual';
    children?: React.ReactNode;
    distance?: number;
    disabled?: boolean;
    isGlobal?: boolean;
    projectId?: string;
};
export default function PopoverList<Item extends {
    user: User;
}>({ items, position, distance, trigger, renderItem, children, disabled, isGlobal, projectId }: Props<Item>): JSX.Element;
export {};
