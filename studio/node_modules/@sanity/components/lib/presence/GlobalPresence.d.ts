/// <reference types="react" />
import { GlobalPresenceItem } from './types';
interface Props {
    projectId: string;
    maxAvatars?: number;
    presence: GlobalPresenceItem[];
}
export declare function GlobalPresence({ projectId, presence, maxAvatars }: Props): JSX.Element;
export {};
