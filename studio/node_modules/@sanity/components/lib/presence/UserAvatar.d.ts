/// <reference types="react" />
import { Position, Size, Status } from './types';
import { User } from './types';
export declare type Props = {
    user: User;
    color?: string;
    position?: Position;
    animateArrowFrom?: Position;
    status?: Status;
    size?: Size;
    tone?: 'navbar';
};
export default function UserAvatar({ user, animateArrowFrom, position, color, status, size, tone }: Props): JSX.Element;
