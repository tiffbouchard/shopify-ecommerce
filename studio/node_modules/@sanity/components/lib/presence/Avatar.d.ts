import React from 'react';
import { Position, Status, Size } from './types';
declare type Props = {
    borderColor: string;
    imageUrl?: string;
    label: string;
    isAnimating?: boolean;
    children?: React.ReactNode;
    onImageLoadError?: (event: Error) => void;
    position?: Position;
    animateArrowFrom?: Position;
    status?: Status;
    size?: Size;
    tone?: 'navbar';
};
export default function Avatar({ borderColor, imageUrl, label, isAnimating, children, onImageLoadError, position, animateArrowFrom, status, size, tone }: Props): JSX.Element;
export {};
