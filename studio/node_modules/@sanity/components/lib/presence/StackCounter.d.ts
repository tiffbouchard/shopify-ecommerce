/// <reference types="react" />
declare type Props = {
    count: number;
    isGlobal?: boolean;
    tone?: 'navbar';
};
export default function StackCounter({ count, isGlobal, tone }: Props): JSX.Element;
export {};
