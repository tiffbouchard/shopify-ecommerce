/// <reference types="react" />
import { Size, Status, User, Location } from './types';
declare type Props = {
    user: User;
    status: Status;
    size?: Size;
    locations?: Location[];
    hasLink?: string | undefined;
};
export declare function PresenceListItem({ user, status, size, locations, hasLink }: Props): JSX.Element;
export {};
