/// <reference types="react" />
declare type Props = {
    label: string;
    item: object;
    onChange?: (any: any) => void;
    onFocus: () => void;
    checked: boolean;
    disabled: boolean;
    name: string;
};
export default function RadioButton({ item, disabled, checked, label, name, onChange, onFocus }: Props): JSX.Element;
export {};
