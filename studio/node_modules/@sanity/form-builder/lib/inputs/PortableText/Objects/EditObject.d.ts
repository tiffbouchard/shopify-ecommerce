/// <reference types="react" />
import { PortableTextBlock } from '@sanity/portable-text-editor';
import { Marker, Presence } from '../../../typedefs';
import { Path } from '../../../typedefs/path';
import { PatchEvent } from '../../../PatchEvent';
import { ObjectEditData } from '../types';
interface Props {
    focusPath: Path;
    markers: Marker[];
    objectEditData: ObjectEditData;
    onBlur: () => void;
    onChange: (patchEvent: PatchEvent, editPath: Path) => void;
    onClose: () => void;
    onFocus: (arg0: Path) => void;
    presence: Presence[];
    readOnly: boolean;
    value: PortableTextBlock[] | undefined;
}
export declare const EditObject: ({ focusPath, markers, objectEditData, onBlur, onChange, onClose, onFocus, presence, readOnly, value }: Props) => JSX.Element;
export {};
