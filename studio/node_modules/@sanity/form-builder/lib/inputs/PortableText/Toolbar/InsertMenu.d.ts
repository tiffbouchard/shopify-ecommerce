/// <reference types="react" />
import { BlockItem } from './types';
declare type Props = {
    disabled: boolean;
    items: BlockItem[];
    readOnly: boolean;
};
export default function InsertMenu(props: Props): JSX.Element;
export {};
