import React from 'react';
import Select from 'part:@sanity/components/selects/default';
import RadioSelect from 'part:@sanity/components/selects/radio';
import { Props } from './types';
export default class SelectInput extends React.Component<Props> {
    _input: (RadioSelect | Select) | null;
    name: string;
    static defaultProps: {
        value: string;
    };
    handleChange: (item: Record<string, any>) => void;
    focus(): void;
    setInput: (input: (RadioSelect | Select) | null) => void;
    render(): JSX.Element;
}
