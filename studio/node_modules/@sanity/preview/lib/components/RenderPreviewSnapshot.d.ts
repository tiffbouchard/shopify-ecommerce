/// <reference types="react" />
import { Type } from '../types';
declare type Props = {
    snapshot: object;
    type: Type;
    isLive: boolean;
    layout: string;
};
export default function RenderPreviewSnapshot(props: Props): JSX.Element;
export {};
